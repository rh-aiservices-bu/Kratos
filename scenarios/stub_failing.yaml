name: stub_failing
description: "Phase 1 stub scenario — fails mid-way; cleanup must still run"
config: {}
tasks:
  - name: stub_pass
    params: {}
  - name: stub_fail
    params: {}
assertions: {}
cleanup: automatic
