name: multi_key_load
description: "Multi-key load test — N keys, M requests distributed evenly across the key pool"
category: "Load Testing"
config:
  key_count: 5
  request_count: 5
  concurrency: 5
  prompt: "Hello, world!"
  subscription: ""           # optional — leave blank for auto-selection
  target_model_name: ""      # optional — blank uses DEFAULT_MODEL; model picker activates when both present
  target_model_namespace: "" # paired with target_model_name
tasks:
  - name: provision_api_key
    params:
      key_name: "maaspal-multi-key"
      count: "${config.key_count}"
      subscription: "${config.subscription}"
  - name: send_requests
    params:
      count: "${config.request_count}"
      concurrency: "${config.concurrency}"
      prompt: "${config.prompt}"
      model: "${config.target_model_namespace}/${config.target_model_name}"
      key_pool: true
    # promql pass-through form (ADR-015) — see single_key_load.yaml.
    assertions:
      error_rate_pct:
        promql: "${harness.inference_results.error_rate_pct}"
        expect: "< 5"
cleanup: automatic
